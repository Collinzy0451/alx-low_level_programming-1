#include <stdio.h>
/**
 * main - point of entry
 * Return: 0 (success)
 */

int main(void)
{

	char hexDigits[] = "0123456789abcdef";

	for (int i = 0; i < 16; i++)
	{
		putchar(hexDigits[i];
				}
	putchar('\n');
	return (0);
}
