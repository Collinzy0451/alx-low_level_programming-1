#include <stdio.h>
/**
 * main - point of entry
 * Return: 0 (success)
 */

int main(void)
{
	int s;

	char t;

	s = 0;
	t = 'a';
	while
		(s <= 9)
		{
			putchar(s);
			s++;
		}
	while
		(t <= 'f')
		{
			putchar(t);
			t++;
		}
	putchar('\n');
	return (0);
}
