#include <stdio.h>
/**
 * main - point of entry
 * Return: 0 (success)
 */

int main(void)
{
	char b;
	int c;


	for (c = 0; c < 10; c++)
		{
			putchar(c);
		}

	for (b = 'a'; b <= 'f'  'b'++)
		{
			putchar(b);
		}
	putchar('\n');
	return (0);
}
